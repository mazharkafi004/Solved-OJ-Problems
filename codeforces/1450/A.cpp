#include<bits/stdc++.h>

using namespace std;

main() {
    int t;
    cin>>t;
    while(t--) {
        long long n;
        cin>>n;
        string s;
        cin>>s;
      sort(s.begin(),s.end());
      cout<<s<<endl;
    }
}